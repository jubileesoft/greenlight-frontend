import Route from '@ember/routing/route';

export default class ProtectedAdminAppsId extends Route.extend({
  // anything which *must* be merged to prototype here
}) {
  model(params: any) {
    return null;
  }
}
